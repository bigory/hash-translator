server:
  port: 8089

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=hashtranslator_users
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true

logging:
  level:
    root: debug


